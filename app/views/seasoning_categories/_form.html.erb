<%= form_for(@seasoning_category) do |f| %>
  <% if @seasoning_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seasoning_category.errors.count, "error") %> prohibited this seasoning_category from being saved:</h2>

      <ul>
      <% @seasoning_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
